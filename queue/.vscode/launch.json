{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "python",
      "request": "launch",
      "name": "Debug queue",
      "module": "celery",
      "args": ["worker", "-A", "tasks", "-l", "info"],
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
